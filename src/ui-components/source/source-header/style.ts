import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    listItem: {
        backgroundColor: "#fff",
        display: "flex",
        justifyContent: "space-between"
    }
})
